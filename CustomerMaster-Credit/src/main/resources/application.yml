server:
  port: 8082
  servlet:
    context-path: /credit-service
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: root
    application:
      name: credit-service

  jpa:
    database: postgresql
    show-sql: true
    hibernate:
      ddl-auto: update

